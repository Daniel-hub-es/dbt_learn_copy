models:
  - name: dim_customers
    # config:
    #   group: customers
    #   contract:
    #     enforced: true
    columns:
      - name: customer_id
        data_type: integer
      - name: first_name
        data_type: string
      - name: last_name
        data_type: string
      - name: most_recent_order_date
        data_type: date
      - name: number_of_orders
        data_type: integer
      - name: lifetime_value
        data_type: float
        